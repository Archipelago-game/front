import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    label: {
      text: {
        main: "#ffffff",
        light: "#f5f5f5",
        dark: "#000000",
      },
      background: {
        main: "#000",
        light: "#666",
        dark: "#333",
      },
    },
  },
});
