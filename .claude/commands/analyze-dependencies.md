---
description: Анализ зависимостей проекта с выявлением неиспользуемых пакетов, устаревших версий и проблем. Использование /analyze-dependencies [--deep]
---

Выполни полный анализ зависимостей проекта.

**Опции:**
- `--deep` - глубокий анализ с проверкой импортов в коде (опционально: {{arg}})

## Что нужно сделать

### 1. Анализ package.json

- Прочитай `package.json`
- Выведи список всех зависимостей (dependencies)
- Выведи список всех dev-зависимостей (devDependencies)
- Проверь наличие peer dependencies
- Определи общее количество зависимостей

### 2. Проверка версий

- Для каждой зависимости определи текущую версию
- Проверь, есть ли более новые версии (используй `npm outdated` или аналогичные команды)
- Выяви критически устаревшие пакеты
- Определи пакеты с мажорными обновлениями
- Определи пакеты с минорными/патч обновлениями

### 3. Анализ использования (если указан --deep)

- Найди все файлы с импортами (`import` и `require`)
- Для каждой зависимости из package.json проверь, используется ли она в коде
- Найди импорты, которые не указаны в package.json
- Выяви неиспользуемые зависимости

### 4. Анализ структуры зависимостей

- Проверь дублирование зависимостей в dependencies и devDependencies
- Найди транзитивные зависимости (зависимости зависимостей)
- Определи размер node_modules (если есть)
- Выяви потенциально опасные пакеты (deprecated, с известными уязвимостями)

### 5. Граф зависимостей (если указан --deep)

- Построй текстовый или mermaid-граф основных зависимостей
- Покажи связи между пакетами
- Выяви циклические зависимости, если есть

### 6. Проверка безопасности

- Запусти `npm audit` или `yarn audit` (если доступно)
- Выведи найденные уязвимости по уровням (critical, high, moderate, low)
- Дай рекомендации по исправлению

### 7. Специфичные для проекта проверки

Для React/Vue/Angular проектов:
- Проверь совместимость версий основных библиотек
- Найди конфликты между версиями

Для TypeScript проектов:
- Проверь наличие @types/* пакетов для библиотек
- Найди отсутствующие типы

Для монорепозиториев:
- Проанализируй workspace-зависимости
- Проверь консистентность версий между пакетами

## Формат отчета

Выведи результаты в следующем формате:

```markdown
# Анализ зависимостей проекта

Дата: [текущая дата]
Проект: [название из package.json]

---

## 📊 Общая статистика

- Всего зависимостей: X
- Production зависимостей: X
- Dev зависимостей: X
- Peer зависимостей: X

## 📦 Основные зависимости

### Production Dependencies
| Пакет | Текущая версия | Последняя версия | Статус |
|-------|----------------|------------------|--------|
| ...   | ...            | ...              | ✅/⚠️/🔴 |

### Dev Dependencies
| Пакет | Текущая версия | Последняя версия | Статус |
|-------|----------------|------------------|--------|
| ...   | ...            | ...              | ✅/⚠️/🔴 |

## ⚠️ Устаревшие пакеты

### Критические обновления (major)
- [список с описанием breaking changes если известны]

### Рекомендуемые обновления (minor/patch)
- [список]

## 🔍 Анализ использования (--deep)

### Неиспользуемые зависимости
- [список пакетов, которые установлены но не импортируются]

### Отсутствующие зависимости
- [импорты без соответствующих пакетов в package.json]

### Дублирование
- [пакеты в обоих разделах dependencies и devDependencies]

## 🔒 Безопасность

### Уязвимости
- Critical: X
- High: X
- Moderate: X
- Low: X

[Детали каждой уязвимости]

## 📈 Граф зависимостей (--deep)

```mermaid
graph TD
    [диаграмма основных зависимостей]
```

## 🔄 Циклические зависимости

[список обнаруженных циклов]

## 💡 Рекомендации

1. **Обновления:**
   - [приоритетные обновления с обоснованием]

2. **Очистка:**
   - [пакеты к удалению]

3. **Безопасность:**
   - [действия по устранению уязвимостей]

4. **Оптимизация:**
   - [предложения по уменьшению размера bundle]

## 📝 Команды для выполнения

```bash
# Обновление пакетов
npm update [пакеты]

# Удаление неиспользуемых
npm uninstall [пакеты]

# Исправление уязвимостей
npm audit fix
```

---

## Детальный анализ (опционально)

[Дополнительная информация по запросу]
```

## Важные указания

- Используй **реальные данные** из проекта
- Проверяй **актуальные версии** пакетов из npm registry
- Будь **конкретным** в рекомендациях
- Отмечай **критичные** проблемы красным флагом 🔴
- Используй **эмодзи** для лучшей читаемости отчета
- Группируй проблемы по **приоритетам**
- Давай **практические команды** для исправления

## Примеры команд для анализа

```bash
# Проверка устаревших пакетов
npm outdated

# Аудит безопасности
npm audit

# Список всех зависимостей
npm list --depth=0

# Проверка дубликатов
npm dedupe --dry-run
```

После завершения анализа предложи следующие шаги для улучшения состояния зависимостей.
